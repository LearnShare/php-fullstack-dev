# 2.16 错误与异常处理

## 目标

- 理解 PHP 错误类型、错误报告机制、日志记录方法。
- 熟悉异常体系（`Exception`、`Error`、`Throwable`）及自定义异常。
- 构建标准的错误处理流程（捕获、记录、反馈）。

## 章节内容

本章分为两个独立小节，每节提供详细的概念解释、语法说明、参数列表和完整示例：

1. **[错误处理](section-01-error-handling.md)**：错误级别、错误报告设置、错误处理器（`set_error_handler()`）、错误日志、环境配置及完整示例。

2. **[异常处理](section-02-exceptions.md)**：异常体系（`Throwable`、`Exception`、`Error`）、`try/catch/finally`、自定义异常、异常传播、异常包装及完整示例。

## 错误级别

- **致命错误** (`E_ERROR`)：脚本终止，如调用未定义函数。
- **警告** (`E_WARNING`)：输出警告，脚本继续。
- **通知** (`E_NOTICE`)：轻微问题，如未定义变量。
- **可捕获错误** (`E_RECOVERABLE_ERROR`)：可被 `try/catch` 捕获。

## 异常体系

- 所有可捕获异常实现 `Throwable` 接口。
- `Exception`：传统异常；`Error`：执行时错误（如类型错误）。
- 自定义异常：继承 `Exception` 或其子类。

## 学习建议

1. **按顺序学习**：先理解错误处理，再学习异常处理。

2. **重点掌握**：
   - 错误级别的设置
   - 自定义错误处理器
   - 异常的类型和处理
   - `finally` 块的使用

3. **实践练习**：
   - 完成每小节后的练习题目
   - 实现实际的错误和异常处理
   - 配置错误日志和报告

## 完成本章后

- 能够正确配置错误报告和处理。
- 理解异常体系和使用方法。
- 能够创建自定义异常。
- 能够在实际项目中实现完善的错误处理。

## 相关章节

- **2.9 控制结构**：了解 `try/catch` 的控制流。
- **阶段三：面向对象、架构与设计模式**：深入学习异常体系设计。
