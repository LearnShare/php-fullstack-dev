# 2.8 数组完整指南

## 目标

- 掌握索引数组、关联数组、嵌套数组的创建与访问。
- 熟悉常用数组函数（遍历、过滤、映射、排序、解构）。
- 能在 JSON、表单、配置文件等场景中高效处理数组数据。
- 理解 PHP 数组与 JavaScript 数组/对象的差异。

## 章节内容

本章分为五个独立小节，每节提供详细的概念解释、语法说明、参数列表和完整示例：

1. **[2.8.1 数组基础](section-01-array-basics.md)**：数组特性、创建方式、访问元素、修改删除、嵌套数组、类型检测、键的类型及完整示例。

2. **[2.8.2 数组遍历](section-02-array-iteration.md)**：`foreach` 循环、引用遍历、嵌套数组遍历、数组指针操作（`reset()`、`next()`、`prev()`、`end()`、`current()`、`key()`）及注意事项。

3. **[2.8.3 数组操作函数](section-03-array-functions.md)**：查找函数（`array_key_exists()`、`in_array()`、`array_search()`、`array_keys()`、`array_values()`）、合并函数（`array_merge()`、`array_merge_recursive()`）、高阶函数（`array_map()`、`array_filter()`、`array_reduce()`）、切片函数（`array_slice()`）、PHP 8.4+ 新函数（`array_first()`、`array_last()`）。

4. **[2.8.4 数组排序](section-04-array-sorting.md)**：基于值的排序（`sort()`、`rsort()`）、保留键的排序（`asort()`、`arsort()`）、基于键的排序（`ksort()`、`krsort()`）、自定义排序（`usort()`、`uasort()`、`uksort()`）、多字段排序、排序标志及完整示例。

5. **[2.8.5 数组解构](section-05-array-destructuring.md)**：索引数组解构、关联数组解构、跳过元素、使用默认值、交换变量、函数返回值解构、嵌套解构、在循环中使用、命名参数解构（PHP 8.1+）及完整示例。

## 数组基础

- PHP 数组是有序映射，可同时包含数字键与字符串键。
- 创建语法：`[]` 或 `array()`。
- 读取元素：`$arr['key']`、`$arr[0]`。

## 与 JavaScript 对比

如果你熟悉 JavaScript，理解 PHP 数组的关键差异：

### 关键差异

1. **统一数据结构**：PHP 数组同时扮演 JavaScript 中数组和对象的角色
2. **键的类型**：PHP 数组键可以是整数或字符串，JavaScript 数组键只能是数字
3. **函数式编程**：PHP 的高阶函数需要将数组作为第二个参数，JavaScript 是方法调用
4. **类型检查**：PHP 8+ 支持类型声明，但数组仍然是动态的

## 学习建议

1. **按顺序学习**：按顺序学习五个小节，理解数组的基本操作和高级特性。

2. **重点掌握**：
   - 数组的创建和访问方式
   - `foreach` 循环和引用遍历
   - 高阶函数的使用（`array_map`、`array_filter`、`array_reduce`）
   - 数组排序的各种方法
   - 数组解构的语法和用法

3. **实践练习**：
   - 完成每小节后的练习题目
   - 实现实际的数据处理功能
   - 处理 JSON 和配置文件

## 完成本章后

- 能够创建和操作各种类型的数组。
- 掌握数组遍历的各种方式。
- 熟练使用数组操作函数处理数据。
- 能够对数组进行各种排序操作。
- 理解并能够使用数组解构。
- 能够在实际项目中高效处理数组数据。

## 相关章节

- **2.4 数据类型**：了解数组类型的基本特性。
- **2.6 表达式与运算符**：了解数组相关的运算符。
- **2.7 字符串操作**：了解字符串与数组的转换。
